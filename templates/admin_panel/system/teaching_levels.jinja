{% extends "base.jinja" %}

{% block title %}Teaching Levels - Admin Panel{% endblock %}

{% block content %}
<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <div class="flex justify-between items-center mb-6">
        <div>
            <h1 class="text-3xl font-bold text-gray-900">Teaching Levels</h1>
            <p class="mt-1 text-sm text-gray-500">
                These levels are currently defined within tutor profiles and are used for filtering and reporting.
                Custom management will be added in a future update.
            </p>
        </div>
        <a href="{{ url('admin_panel:global_dashboard') }}" class="bg-gray-200 text-gray-700 px-4 py-2 rounded-md hover:bg-gray-300">
            ← Back to Dashboard
        </a>
    </div>

    <div class="bg-white rounded-lg shadow overflow-hidden">
        <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
                <tr>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                        Value
                    </th>
                    <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                        Display Name
                    </th>
                </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-200">
                {% for value, label in teaching_levels %}
                <tr class="hover:bg-gray-50">
                    <td class="px-6 py-4 whitespace-nowrap text-sm font-mono text-gray-900">{{ value }}</td>
                    <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{ label }}</td>
                </tr>
                {% else %}
                <tr>
                    <td colspan="2" class="px-6 py-4 text-center text-gray-500">
                        No teaching levels are defined yet.
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    <div class="mt-6 bg-indigo-50 border border-indigo-100 text-indigo-700 px-4 py-3 rounded-md text-sm">
        <strong>Coming Soon:</strong> full teaching level management (add, edit, archive) will be introduced in a future release.
    </div>
</div>
{% endblock %}

