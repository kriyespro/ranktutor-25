{% extends "base.jinja" %}

{% block title %}Student Dashboard - RankTutor{% endblock %}

{% block content %}
<div class="max-w-7xl mx-auto px-3 sm:px-4 lg:px-8 py-4 sm:py-6 lg:py-8">
    <div class="mb-6 sm:mb-8">
        <h1 class="text-3xl sm:text-4xl font-extrabold text-gray-900 mb-4 tracking-tight">Student Dashboard</h1>
        {% include "components/student_menu.jinja" %}
    </div>
    
    <!-- Stats Cards Row -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-3 sm:gap-4 mb-4 sm:mb-6">
        <!-- Wallet Balance Card -->
        {% if user.wallet %}
        <div class="bg-gradient-to-br from-emerald-500 to-emerald-600 rounded-xl shadow-lg p-3 sm:p-4 transform hover:scale-105 transition-transform border-2 border-emerald-400">
            <div class="flex items-center justify-between mb-2">
                <div class="text-2xl">💰</div>
                <div class="w-8 h-8 rounded-lg bg-white/20 backdrop-blur-sm flex items-center justify-center">
                    <span class="text-lg">💳</span>
                </div>
            </div>
            <p class="text-xs sm:text-sm text-emerald-50 font-semibold mb-1">Wallet Balance</p>
            <p class="text-xl sm:text-2xl font-extrabold text-white mb-1">₹{{ user.wallet.balance|floatformat(2) }}</p>
            <p class="text-xs text-emerald-50/90 mb-2">Payments auto-deduct after each class</p>
            <a href="{{ url('payments:wallet') }}" class="inline-flex items-center justify-center gap-1 bg-white text-emerald-600 px-3 py-1.5 rounded-lg hover:bg-emerald-50 text-xs font-bold transition-all shadow-md hover:shadow-lg transform hover:scale-105 w-full">
                <span>💳</span>
                <span>Recharge</span>
            </a>
        </div>
        {% endif %}
        
        <!-- Total Bookings Card -->
        <div class="bg-white rounded-xl shadow-lg p-3 sm:p-4 border-2 border-gray-100 hover:border-purple-200 transform hover:scale-105 transition-all">
            <div class="flex items-center justify-between mb-2">
                <div class="text-2xl">📚</div>
                <div class="w-8 h-8 rounded-lg bg-purple-100 flex items-center justify-center">
                    <span class="text-lg text-purple-600">📅</span>
                </div>
            </div>
            <p class="text-xs sm:text-sm text-gray-600 font-semibold mb-1">Total Bookings</p>
            <p class="text-xl sm:text-2xl font-extrabold text-gray-900 mb-1">{{ upcoming_bookings|length + (past_bookings|length if past_bookings else 0) }}</p>
            <p class="text-xs text-gray-500">All your lessons</p>
        </div>
        
        <!-- Upcoming Classes Card -->
        <div class="bg-white rounded-xl shadow-lg p-3 sm:p-4 border-2 border-gray-100 hover:border-blue-200 transform hover:scale-105 transition-all">
            <div class="flex items-center justify-between mb-2">
                <div class="text-2xl">📅</div>
                <div class="w-8 h-8 rounded-lg bg-blue-100 flex items-center justify-center">
                    <span class="text-lg text-blue-600">⏰</span>
                </div>
            </div>
            <p class="text-xs sm:text-sm text-gray-600 font-semibold mb-1">Upcoming Classes</p>
            <p class="text-xl sm:text-2xl font-extrabold text-gray-900 mb-1">{{ upcoming_bookings|length }}</p>
            <p class="text-xs text-gray-500">Scheduled lessons</p>
        </div>
        
        <!-- Active Tutors Card -->
        <div class="bg-white rounded-xl shadow-lg p-3 sm:p-4 border-2 border-gray-100 hover:border-purple-200 transform hover:scale-105 transition-all">
            <div class="flex items-center justify-between mb-2">
                <div class="text-2xl">👨‍🏫</div>
                <div class="w-8 h-8 rounded-lg bg-purple-100 flex items-center justify-center">
                    <span class="text-lg text-purple-600">⭐</span>
                </div>
            </div>
            <p class="text-xs sm:text-sm text-gray-600 font-semibold mb-1">Active Tutors</p>
            <p class="text-xl sm:text-2xl font-extrabold text-gray-900 mb-1">{% set unique_tutors = upcoming_bookings|map(attribute='tutor')|unique|list %}{{ unique_tutors|length }}</p>
            <p class="text-xs text-gray-500">Currently learning from</p>
        </div>
    </div>
    
    {% if ai_recommendations %}
    <div class="bg-gradient-to-br from-purple-50 via-purple-100 to-indigo-50 rounded-2xl shadow-xl p-5 sm:p-6 lg:p-8 mb-6 sm:mb-8 border-2 border-purple-200">
        <div class="flex items-center justify-between mb-5 sm:mb-6">
            <h2 class="text-xl sm:text-2xl font-extrabold text-gray-900 flex items-center gap-2">
                <span class="text-2xl sm:text-3xl">🎯</span>
                <span>Recommended Tutors for You</span>
            </h2>
        </div>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-5">
            {% for tutor in ai_recommendations %}
            <div class="bg-white rounded-xl p-4 sm:p-5 shadow-lg hover:shadow-xl transition-all border-2 border-gray-100 hover:border-purple-300 transform hover:scale-105">
                <h3 class="text-lg sm:text-xl font-bold mb-2 text-gray-900">{{ tutor.user.get_full_name()|default(tutor.user.username) }}</h3>
                <p class="text-sm sm:text-base text-gray-600 mb-3 font-medium">📍 {{ tutor.city }}, {{ tutor.state }}</p>
                <div class="flex items-center justify-between pt-3 border-t border-gray-100">
                    <span class="inline-flex items-center gap-1 px-3 py-1 bg-gradient-to-r from-amber-100 to-amber-200 text-amber-800 border-2 border-amber-300 rounded-xl text-sm font-bold">★ {{ tutor.average_rating|default('0.0') }}</span>
                    <a href="{{ url('tutors:detail', tutor.id) }}" class="text-purple-600 hover:text-purple-800 text-sm sm:text-base font-bold min-h-[44px] flex items-center transition-colors">View →</a>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
    {% endif %}
    
    <!-- Bookings Needing Payment -->
    {% if bookings_needing_payment %}
    <div class="bg-gradient-to-br from-amber-50 via-yellow-50 to-orange-50 border-2 border-amber-300 rounded-2xl shadow-xl p-5 sm:p-6 lg:p-8 mb-6 sm:mb-8">
        <h2 class="text-xl sm:text-2xl font-extrabold text-amber-900 mb-4 sm:mb-6 flex items-center gap-3">
            <span class="text-3xl sm:text-4xl">💳</span>
            <span>Payment Required ({{ bookings_needing_payment|length }})</span>
        </h2>
        <div class="space-y-4 sm:space-y-5">
            {% for booking in bookings_needing_payment %}
            <div class="bg-white rounded-xl p-5 sm:p-6 border-2 border-amber-300 hover:shadow-xl transition-all transform hover:scale-[1.01]">
                <div class="flex flex-col sm:flex-row sm:justify-between sm:items-start gap-5">
                    <div class="flex-1 min-w-0">
                        <h3 class="text-lg sm:text-xl font-extrabold text-gray-900 mb-4 flex items-center gap-2">
                            <span class="text-2xl">👨‍🏫</span>
                            <span>{{ booking.tutor.get_full_name()|default(booking.tutor.username) }}</span>
                        </h3>
                        <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 text-sm sm:text-base text-gray-700 mb-4">
                            <p class="font-semibold"><span class="text-lg">📚</span> Subject: <span class="font-bold">{{ booking.subject.name }}</span></p>
                            <p class="font-semibold"><span class="text-lg">📅</span> Date: <span class="font-bold">{{ booking.lesson_date|date('M d, Y') }}</span></p>
                            <p class="font-semibold"><span class="text-lg">⏰</span> Time: <span class="font-bold">{{ booking.lesson_time|date('h:i A') }}</span></p>
                            <p class="font-semibold"><span class="text-lg">⏱️</span> Duration: <span class="font-bold">{{ booking.duration_hours }} hour{{ 's' if booking.duration_hours != 1 else '' }}</span></p>
                        </div>
                        <div class="mt-4 p-4 bg-gradient-to-r from-purple-50 to-purple-100 rounded-xl border-2 border-purple-200">
                            <p class="text-xl sm:text-2xl font-extrabold text-purple-700 flex items-center gap-2">
                                <span class="text-2xl">💰</span>
                                <span>Amount: ₹{{ booking.total_amount|floatformat(2) }}</span>
                            </p>
                        </div>
                    </div>
                    <div class="flex flex-col gap-3 sm:ml-4 sm:flex-shrink-0 w-full sm:w-auto">
                        <a href="{{ url('payments:process', booking_id=booking.id) }}" class="w-full inline-flex items-center justify-center gap-2 bg-gradient-to-r from-emerald-500 to-emerald-600 text-white px-6 sm:px-8 py-3.5 sm:py-4 rounded-xl hover:from-emerald-600 hover:to-emerald-700 text-base sm:text-lg font-bold whitespace-nowrap min-h-[52px] transition-all shadow-lg hover:shadow-xl transform hover:scale-105">
                            <span class="text-xl">💳</span>
                            <span>Pay Now</span>
                        </a>
                        <a href="{{ url('bookings:detail', booking_id=booking.id) }}" class="w-full inline-flex items-center justify-center gap-2 text-purple-700 hover:text-purple-900 text-sm sm:text-base text-center border-2 border-purple-300 px-4 py-2.5 rounded-xl min-h-[44px] font-bold hover:bg-purple-50 transition-all shadow-md">
                            <span>👁️</span>
                            <span>View Details</span>
                        </a>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
    {% endif %}
    
    <!-- Bookings Needing Review -->
    {% if bookings_needing_review %}
    <div class="bg-blue-50 border-l-4 border-blue-400 rounded-lg shadow p-4 sm:p-5 lg:p-6 mb-4 sm:mb-6">
        <h2 class="text-lg sm:text-xl font-semibold text-blue-900 mb-3 sm:mb-4">
            ⭐ Leave a Review ({{ bookings_needing_review|length }})
        </h2>
        <div class="space-y-3 sm:space-y-4">
            {% for booking in bookings_needing_review %}
            <div class="bg-white rounded-lg p-3 sm:p-4 border border-blue-200">
                <div class="flex flex-col sm:flex-row sm:justify-between sm:items-start gap-3">
                    <div class="flex-1 min-w-0">
                        <h3 class="text-base sm:text-lg font-semibold text-gray-900 mb-2">
                            {{ booking.tutor.get_full_name()|default(booking.tutor.username) }}
                        </h3>
                        <p class="text-sm sm:text-base text-gray-600 mb-1">
                            <strong>Subject:</strong> {{ booking.subject.name }}
                        </p>
                        <p class="text-sm sm:text-base text-gray-600 mb-1">
                            <strong>Lesson Date:</strong> {{ booking.lesson_date|date('F d, Y') }}
                        </p>
                        {% if booking.completed_at %}
                        <p class="text-xs sm:text-sm text-gray-500 mt-2">
                            Completed: {{ booking.completed_at|date('M d, Y') }}
                        </p>
                        {% endif %}
                    </div>
                    <div class="flex flex-col gap-2 sm:ml-4 sm:flex-shrink-0">
                        <a href="{{ url('reviews:create', booking_id=booking.id) }}" class="w-full sm:w-auto inline-flex items-center justify-center bg-blue-600 text-white px-5 sm:px-6 py-2.5 sm:py-3 rounded-md hover:bg-blue-700 text-center text-sm sm:text-base font-medium whitespace-nowrap min-h-[44px] transition-colors">
                            Leave Review
                        </a>
                        <a href="{{ url('bookings:detail', booking_id=booking.id) }}" class="w-full sm:w-auto inline-flex items-center justify-center text-indigo-600 hover:text-indigo-800 text-sm sm:text-base text-center border border-indigo-200 px-4 py-2 rounded-md min-h-[44px]">
                            View Details
                        </a>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
    {% endif %}
    
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 sm:gap-8 mb-6 sm:mb-8">
        <div class="bg-gradient-to-br from-blue-50 via-indigo-50 to-purple-50 rounded-2xl shadow-xl p-5 sm:p-6 lg:p-8 border-2 border-blue-200">
            <h2 class="text-xl sm:text-2xl font-extrabold mb-5 sm:mb-6 flex items-center gap-3 text-blue-900">
                <span class="text-3xl sm:text-4xl">📅</span>
                <span>Upcoming Lessons</span>
            </h2>
            {% if upcoming_bookings %}
                <div class="space-y-4 sm:space-y-5">
                    {% for booking in upcoming_bookings %}
                    <div class="bg-white rounded-xl p-4 sm:p-5 border-2 border-blue-200 hover:shadow-lg transition-all transform hover:scale-[1.01]">
                        <div class="flex flex-col sm:flex-row sm:justify-between sm:items-start gap-4">
                            <div class="flex-1 min-w-0">
                                <p class="text-base sm:text-lg font-extrabold text-gray-900 mb-3 flex items-center gap-2">
                                    <span class="text-xl">👨‍🏫</span>
                                    <span>{{ booking.tutor.get_full_name()|default(booking.tutor.username) }}</span>
                                </p>
                                <p class="text-sm sm:text-base text-gray-700 mb-2 flex items-center gap-2 font-medium">
                                    <span class="text-lg">📅</span>
                                    <span>{{ booking.lesson_date|date('M d, Y') }}</span>
                                </p>
                                <p class="text-sm sm:text-base text-gray-700 mb-2 flex items-center gap-2 font-medium">
                                    <span class="text-lg">⏰</span>
                                    <span>{{ booking.lesson_time|date('h:i A') }}</span>
                                </p>
                                <p class="text-xs sm:text-sm text-gray-600 mb-3 font-semibold">
                                    <span class="text-base">📚</span> {{ booking.subject.name }} - {{ booking.get_mode_display() }}
                                </p>
                                <span class="inline-block px-3 py-1.5 bg-{% if booking.status == 'accepted' %}emerald{% elif booking.status == 'pending' %}amber{% else %}gray{% endif %}-100 text-{% if booking.status == 'accepted' %}emerald{% elif booking.status == 'pending' %}amber{% else %}gray{% endif %}-800 rounded-xl text-xs sm:text-sm font-bold border border-{% if booking.status == 'accepted' %}emerald{% elif booking.status == 'pending' %}amber{% else %}gray{% endif %}-300">
                                    {{ booking.get_status_display() }}
                                </span>
                            </div>
                            <a href="{{ url('bookings:detail', booking_id=booking.id) }}" class="inline-flex items-center gap-2 px-5 py-2.5 bg-gradient-to-r from-purple-600 to-purple-700 text-white rounded-xl hover:from-purple-700 hover:to-purple-800 text-sm sm:text-base font-bold min-h-[44px] transition-all shadow-md hover:shadow-lg transform hover:scale-105">
                                <span>👁️</span>
                                <span>View</span>
                            </a>
                        </div>
                    </div>
                    {% endfor %}
                </div>
            {% else %}
                <p class="text-base text-gray-500 font-medium text-center py-8">No upcoming lessons</p>
            {% endif %}
        </div>
        
        <div class="bg-gradient-to-br from-gray-50 via-gray-100 to-gray-200 rounded-2xl shadow-xl p-5 sm:p-6 lg:p-8 border-2 border-gray-300">
            <h2 class="text-xl sm:text-2xl font-extrabold mb-5 sm:mb-6 flex items-center gap-3 text-gray-900">
                <span class="text-3xl sm:text-4xl">📚</span>
                <span>Past Lessons</span>
            </h2>
            {% if past_bookings %}
                <div class="space-y-4 sm:space-y-5">
                    {% for booking in past_bookings %}
                    <div class="bg-white rounded-xl p-4 sm:p-5 border-2 border-gray-200 hover:shadow-lg transition-all transform hover:scale-[1.01]">
                        <div class="flex flex-col sm:flex-row sm:justify-between sm:items-start gap-4">
                            <div class="flex-1 min-w-0">
                                <p class="text-base sm:text-lg font-extrabold text-gray-900 mb-3 flex items-center gap-2">
                                    <span class="text-xl">👨‍🏫</span>
                                    <span>{{ booking.tutor.get_full_name()|default(booking.tutor.username) }}</span>
                                </p>
                                <p class="text-sm sm:text-base text-gray-700 mb-2 flex items-center gap-2 font-medium">
                                    <span class="text-lg">📅</span>
                                    <span>{{ booking.lesson_date|date('M d, Y') }}</span>
                                </p>
                                <p class="text-sm sm:text-base text-gray-700 mb-2 flex items-center gap-2 font-medium">
                                    <span class="text-lg">⏰</span>
                                    <span>{{ booking.lesson_time|date('h:i A') }}</span>
                                </p>
                                <p class="text-xs sm:text-sm text-gray-600 font-semibold">
                                    <span class="text-base">📚</span> {{ booking.subject.name }}
                                </p>
                            </div>
                            <a href="{{ url('bookings:detail', booking_id=booking.id) }}" class="inline-flex items-center gap-2 px-5 py-2.5 bg-gradient-to-r from-purple-600 to-purple-700 text-white rounded-xl hover:from-purple-700 hover:to-purple-800 text-sm sm:text-base font-bold min-h-[44px] transition-all shadow-md hover:shadow-lg transform hover:scale-105">
                                <span>👁️</span>
                                <span>View</span>
                            </a>
                        </div>
                    </div>
                    {% endfor %}
                </div>
            {% else %}
                <p class="text-base text-gray-500 font-medium text-center py-8">No past lessons</p>
            {% endif %}
        </div>
    </div>
    
    <!-- Disputes Section -->
    {% if student_disputes %}
    <div class="bg-white rounded-lg shadow p-4 sm:p-5 lg:p-6 mt-4 sm:mt-6">
        <h2 class="text-lg sm:text-xl font-semibold mb-3 sm:mb-4">My Disputes</h2>
        <div class="space-y-3 sm:space-y-4">
            {% for dispute in student_disputes %}
            <div class="border rounded-lg p-3 sm:p-4 {% if dispute.status == 'open' %}border-red-200 bg-red-50{% elif dispute.status == 'under_review' %}border-yellow-200 bg-yellow-50{% elif dispute.status == 'resolved' %}border-green-200 bg-green-50{% else %}border-gray-200 bg-gray-50{% endif %}">
                <div class="flex flex-col sm:flex-row sm:justify-between sm:items-start gap-3">
                    <div class="flex-1 min-w-0">
                        <div class="flex flex-wrap items-center gap-2 sm:gap-3 mb-2">
                            <h3 class="text-base sm:text-lg font-semibold text-gray-900">
                                Dispute #{{ dispute.id }} - {{ dispute.get_dispute_type_display() }}
                            </h3>
                            <span class="px-2.5 sm:px-3 py-1 text-xs font-semibold rounded-full
                                {% if dispute.status == 'open' %}bg-red-100 text-red-800
                                {% elif dispute.status == 'under_review' %}bg-yellow-100 text-yellow-800
                                {% elif dispute.status == 'resolved' %}bg-green-100 text-green-800
                                {% else %}bg-gray-100 text-gray-800{% endif %}">
                                {{ dispute.get_status_display() }}
                            </span>
                        </div>
                        <p class="text-sm sm:text-base text-gray-600 mb-2">
                            <strong>Booking:</strong> {{ dispute.booking.subject.name }} with {{ dispute.booking.tutor.get_full_name()|default(dispute.booking.tutor.username) }}
                        </p>
                        <p class="text-xs sm:text-sm text-gray-700 mb-2">
                            <strong>Description:</strong> {{ dispute.description|truncatewords(30) }}
                        </p>
                        {% if dispute.resolution %}
                        <div class="mt-3 p-2 sm:p-3 bg-white rounded border">
                            <p class="text-xs sm:text-sm font-semibold text-gray-900 mb-1">Resolution:</p>
                            <p class="text-xs sm:text-sm text-gray-700">{{ dispute.resolution }}</p>
                            {% if dispute.resolved_by %}
                            <p class="text-xs text-gray-500 mt-2">
                                Resolved by: {{ dispute.resolved_by.get_full_name()|default(dispute.resolved_by.username) }}
                                {% if dispute.resolved_at %}
                                on {{ dispute.resolved_at|date('M d, Y') }}
                                {% endif %}
                            </p>
                            {% endif %}
                        </div>
                        {% endif %}
                        <p class="text-xs text-gray-500 mt-2">
                            Raised: {{ dispute.created_at|date('M d, Y h:i A') }}
                        </p>
                    </div>
                    <div class="sm:ml-4 sm:flex-shrink-0">
                        <a href="{{ url('reviews:dispute_detail', dispute_id=dispute.id) }}" class="w-full sm:w-auto inline-flex items-center justify-center bg-indigo-600 text-white px-4 sm:px-5 py-2.5 sm:py-3 rounded-md hover:bg-indigo-700 text-sm sm:text-base font-medium whitespace-nowrap min-h-[44px] transition-colors">
                            View Details
                        </a>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
    {% endif %}
    
    <div class="mt-4 sm:mt-6 flex flex-wrap gap-3">
        <a href="{{ url('tutors:search') }}" class="inline-flex items-center justify-center gap-2 bg-gradient-to-r from-indigo-500 to-indigo-600 text-white px-6 sm:px-8 py-3 sm:py-4 rounded-lg hover:from-indigo-600 hover:to-indigo-700 text-base sm:text-lg font-bold min-h-[52px] transition-all shadow-lg hover:shadow-xl transform hover:scale-105">
            <span class="text-xl">🔍</span>
            <span>Find Tutors</span>
        </a>
        <a href="{{ url('students:payments') }}" class="inline-flex items-center justify-center gap-2 bg-gradient-to-r from-green-500 to-green-600 text-white px-6 sm:px-8 py-3 sm:py-4 rounded-lg hover:from-green-600 hover:to-green-700 text-base sm:text-lg font-bold min-h-[52px] transition-all shadow-lg hover:shadow-xl transform hover:scale-105">
            <span class="text-xl">💳</span>
            <span>Payments</span>
        </a>
        <a href="{{ url('students:classes') }}" class="inline-flex items-center justify-center gap-2 bg-gradient-to-r from-blue-500 to-blue-600 text-white px-6 sm:px-8 py-3 sm:py-4 rounded-lg hover:from-blue-600 hover:to-blue-700 text-base sm:text-lg font-bold min-h-[52px] transition-all shadow-lg hover:shadow-xl transform hover:scale-105">
            <span class="text-xl">📚</span>
            <span>All Classes</span>
        </a>
        <a href="{{ url('students:homework') }}" class="inline-flex items-center justify-center gap-2 bg-gradient-to-r from-purple-500 to-purple-600 text-white px-6 sm:px-8 py-3 sm:py-4 rounded-lg hover:from-purple-600 hover:to-purple-700 text-base sm:text-lg font-bold min-h-[52px] transition-all shadow-lg hover:shadow-xl transform hover:scale-105">
            <span class="text-xl">📝</span>
            <span>Homework</span>
        </a>
    </div>
</div>
{% endblock %}

